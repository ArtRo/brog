<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo" xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
    http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd
    http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <dubbo:application name="qt-cosfun-main-provider" owner="tsingteng"
                       organization="tsingteng" />

    <dubbo:registry protocol="zookeeper" address="${dubbo.registry.address}"
                    file="${dubbo.registry.file}" group="${dubbo.registry.group}" />

    <dubbo:annotation package="cn.cosfun.main" />

    <dubbo:consumer check="false" retries="0" />

    <dubbo:provider threads="600" threadpool="fixed"
                    retries="0" loadbalance="roundrobin" accesslog="true" timeout="10000" />
    <dubbo:protocol name="dubbo" port="${dubbo.protocol.dubbo.port}"
                    accesslog="${dubbo.log.file}" />
    <context:property-placeholder
            ignore-resource-not-found="false"
            location="classpath*:db.properties,
            classpath*:dubbo.properties,
            classpath*:app.properties,
            classpath*:redis.properties,
            classpath*:mongoDb.properties" file-encoding="UTF-8"/>

    <import resource="application-db.xml" />
    <import resource="application-redis.xml" />
    <import resource="mongo-spring.xml" />
    <!-- 启动对@AspectJ注解的支持 -->
    <aop:aspectj-autoproxy />
    <bean id="taskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <!-- 线程池维护线程的最少数量 -->
        <property name="corePoolSize" value="10" />
        <!-- 允许的空闲时间 -->
        <property name="keepAliveSeconds" value="200" />
        <!-- 线程池维护线程的最大数量 -->
        <property name="maxPoolSize" value="50" />
        <!-- 缓存队列 -->
        <property name="queueCapacity" value="500" />
        <!-- 对拒绝task的处理策略 -->
        <property name="rejectedExecutionHandler">
            <bean class="java.util.concurrent.ThreadPoolExecutor$CallerRunsPolicy" />
        </property>
    </bean>

</beans>